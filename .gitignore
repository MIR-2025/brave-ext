# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Node (not used at runtime, but keep stray installs out)
node_modules/
npm-debug.log*

# Packaged builds
*.zip
*.crx
*.pem

# Stray screenshots / images dropped at the repo root (extension icons live in
# */icons/ and are added explicitly). This repo is PUBLIC -- keep loose images out.
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.gif
